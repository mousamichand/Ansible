---
  - name: "extra variable demo"
    hosts: all
    connection: local
    vars:
      fruit: "{{mopheus['customOptions']['input1']}}"
    tasks:

    - name: "print message"
      ansible.builtin.debug:
        msg: "fruit is {{ fruit }}"
